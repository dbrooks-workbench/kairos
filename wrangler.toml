name = "kairos"
pages_build_output_dir = "client/web"
compatibility_date = "2024-09-23"

[[kv_namespaces]]
binding = "SESSIONS"
id = "REPLACE_WITH_KV_NAMESPACE_ID"
# preview_id is only needed for `wrangler pages dev` local development — skip for now
