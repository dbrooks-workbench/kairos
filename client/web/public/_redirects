# SPA fallback: serve index.html for client-side routes (/calendar, /events,
# /reminders, …). Cloudflare Pages runs Functions (/auth/*, /api/*) and serves
# existing static assets before applying this catch-all, so they aren't shadowed.
/*    /index.html    200
